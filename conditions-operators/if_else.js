var cls = require('../services/lib').cls
var log = require('../services/lib').log

cls()

var isJSGreat = true

if (isJSGreat) {
    log('Yes it\'s super great!')
} else {
    log('Old schoolers think JavaScript is horrible')
}

